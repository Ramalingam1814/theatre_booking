/* ============================================================
   about.js — Logic for about.html
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  renderNavbar("about");
});
